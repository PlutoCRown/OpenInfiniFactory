# 图形性能重构计划

## 目标与边界

- 本文只规划图形性能工作，当前阶段不执行图形架构改造。
- UI、输入、模拟和 3D 绘制分别观测，禁止只用总 FPS 推断瓶颈。
- 基准统一使用 Release 构建、固定窗口尺寸、固定相机、固定画质和固定存档；Debug 构建只用于定位逻辑问题。
- 优化必须记录修改前后的 FPS、CPU 主线程、Render World、GPU 时间、实体数、draw call、三角形数和显存变化。

## 当前结构判断

游戏 3D 相机已经渲染到 `GameplayViewImage`，UI 相机独立合成；这允许 3D 限帧时复用上一张纹理。静态场景方块已有 16³ chunk 合并，而工厂方块仍以大量实体和模型子实体表达。当前 `/perf` 能拆主世界系统阶段，但不能直接回答 GPU pass、draw call、管线等待和 Render World 开销，因此第一阶段必须先补观测。

## 基准矩阵

每个场景预热 10 秒，采样至少 30 秒，重复三次并记录 P50/P95：

| 场景 | 规模 | 目的 |
|---|---:|---|
| 主菜单空载 | 0 方块 | 窗口提交、UI、后台系统基线 |
| 单草地测试世界 | 1 | 相机、天空盒、后处理固定成本 |
| 基础方块平面 | 10k / 100k | chunk mesh、阴影、可见性上限 |
| 单类工厂方块 | 10k / 100k | 实体、Transform、draw call、阴影成本 |
| 混合工厂世界 | 10k / 100k | 材质切换、模型子节点、特效常态成本 |
| 模拟运行世界 | 10k | 动画、局部刷新和渲染重建尖峰 |

所有场景分别测试：阴影开/关、SSAO 开/关、天空盒开/关、3D 跟随 UI/60/30 FPS。

## 阶段 1：建立可归因的图形观测

1. 给 `/perf` 增加稳定采样窗口，而不是只返回指数平滑后的某一帧。
2. 接入 Bevy Render Diagnostics 或 WGPU 时间戳（平台支持时），至少拆分阴影、深度预通道、主不透明、透明、后处理、UI 合成和 Present。
3. 暴露 draw call、可见实体、三角形、渲染目标分辨率和主要 GPU 资源数量。
4. 给基准响应记录构建 profile、Git revision、设备/后端、窗口物理尺寸、VSync 和画质配置。
5. 用 Instruments Time Profiler 与 Metal System Trace 交叉验证 `/perf`，明确是 CPU submit、GPU、Present 等待还是调度空洞。

验收：能够解释空载 120 FPS 时每个 8.33 ms 的主要去向，未知时间不超过 10%。

## 阶段 2：降低工厂方块的实体和提交成本

1. 按视觉稳定性把方块拆成静态主体、动态部件、状态叠层三类。
2. 静态主体按 mesh/material/chunk 合批，或使用 GPU instancing；动态部件继续保留独立实体。
3. 电线、焊点、面板等重复小部件改为实例批次，避免一个逻辑方块生成多个长期 Render Entity。
4. 维护 `BlockEntityIndex` 到批次实例槽位的映射，编辑时局部更新实例缓冲，不做全世界重建。
5. 用基础方块模型替代全部工厂模型作为诊断开关：若性能显著恢复，优先治理模型子节点/材质；否则优先治理可见性、阴影或提交路径。

验收：10k 静止工厂方块的 Render Entity 和 draw call 不再随模型子节点数线性增长。

## 阶段 3：可见性、阴影和后处理

1. 以 chunk 为粗粒度裁剪单位，远距离使用更大的聚合块或 LOD。
2. 阴影只覆盖会影响画面的方块；小连接件默认不投影，静态阴影与动态阴影分离。
3. 根据 GPU 数据决定是否保留 TAA 所需的 Depth/Normal/MotionVector 预通道；禁止凭感觉删除。
4. SSAO、Bloom、天空盒分别建立 GPU 成本曲线，并针对移动端/Web 设置独立默认值。
5. 增加动态 3D 分辨率档位；UI 始终按窗口原生分辨率合成。

验收：画质档位之间有可量化、单调的 GPU 成本差异；低档不牺牲 UI 清晰度。

## 阶段 4：模拟表现层的增量更新

1. 把每回合的改变格集合按“变换、模型、材质、特效”分类。
2. 只更新受影响实例或 chunk，禁止稳定世界每回合全量收集/重建。
3. 动画结束后把静态对象归并回批次；正在运动的对象暂时脱离批次。
4. 为大量同时移动设置每帧预算和跨帧上传，避免单帧 CPU/GPU 上传尖峰卡住 UI。

验收：模拟回合的渲染同步成本与实际改变方块数相关，而不是与世界总方块数相关。

## 实施顺序

1. 先完成阶段 1，查清空载低帧率原因。
2. 根据数据在阶段 2 与阶段 3 中选择最大瓶颈，不能预设一定是 draw call 或 GPU。
3. 模拟 CPU 优化可与本计划串行推进；只有图形表现阻碍模拟基准时，才启用“全部工厂块替换为基础模型”的临时诊断开关。
4. 每次只合入一个可独立复测的改动，并把基准结果追加到性能报告。
