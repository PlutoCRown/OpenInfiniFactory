# Turn 状态提交与连接刷新时序

本文档记录工厂结构运动后电线连接状态异常的原因，以及模拟状态、表现动画和连接模型应遵循的时序。

## 1. 目标语义

一次模拟 Turn 应该先完成所有模拟阶段和运动提交，再产生本回合最终世界状态。表现层可以播放运动动画，但连接关系不应在每个方块运动过程中重新计算。

连接关系应该以离散的 Turn 状态存在：

1. 动画开始前，所有方块仍使用上一个 Turn 的连接模型；
2. 运动动画播放期间，不重新计算连接拓扑或重建连接子模型；
3. 所有运动动画完成后，对受影响区域统一刷新一次连接模型；
4. 下一次 Turn 使用已经完成刷新的最终连接状态。

## 2. 当前实现

### 2.1 模拟层

模拟层没有持有渲染模型。结构运动时只是逐个修改 `WorldBlocks` 中的方块位置，并更新结构状态。信号缓存在运动前刷新一次，在 Turn 后再刷新一次，不会在每个方块移动后重建。

相关位置：

- 运动前的信号阶段：[core.rs](crates/oif-sim/src/simulation/core.rs:110)
- 运动执行阶段：[core.rs](crates/oif-sim/src/simulation/core.rs:209)
- 运动后的信号刷新：[core.rs](crates/oif-sim/src/simulation/core.rs:261)
- 批量搬迁方块：[structures.rs](crates/oif-sim/src/simulation/structures.rs:1148)

这部分基本符合“一个 Turn 内完成模拟提交”的方向。

### 2.2 表现层

表现层收到 `after` 世界后，会在运动动画开始时立即按最终世界重建移动方块和连接件：

- [incremental.rs](src/scene/incremental.rs:758)
- [incremental.rs](src/scene/incremental.rs:823)
- [incremental.rs](src/scene/incremental.rs:571)

与此同时，实体仍然由 `AnimatedBlock` 从旧位置插值到新位置：

- [animation.rs](src/game/world/animation.rs:428)

动画结束时目前只移除动画组件，没有触发一次“连接模型刷新”：

- [animation.rs](src/game/world/animation.rs:446)

因此运动方块可能还在旧位置时，模型已经按照新位置连接；静止邻居也可能已经按照最终拓扑重建。这会造成运动期间的断线、错接和连接臂穿插。

## 3. 电线状态异常的两个来源

### 3.1 几何连接模型提前切换

工厂连接件和电线臂是在生成表现实体时根据当前世界邻居计算的。当前生成使用的是 Turn 结束后的世界，但生成发生在动画开始时。

因此，运动过程中的连接模型是“最终拓扑”，而实体位置是“动画中的中间位置”。这属于表现时序错误，不是电线 BFS 本身一定错误。

### 3.2 通电状态使用运动前结果

本回合的 `powered_wires` 在运动前计算，运动完成后只是按 `BlockId` 将旧的通电方块映射到新坐标。这样可以保证运动设备使用 Turn 开始时的供电状态，但如果运动改变了电线网络，表现层的亮灭状态可能要到下一回合才反映。

因此需要区分两个结果：

- `control_power`：本回合行为和运动决策使用的供电状态；
- `display_power`：运动完成后表现层应该显示的供电状态。

不能继续让一个含义不明确的 `powered_wires` 同时承担这两个职责。

## 4. 当前缓存和刷新逻辑

信号方块移动时会递增 `topology_revision`，信号缓存根据版本重建：

- [attachments.rs](crates/oif-sim/src/world/grid/attachments.rs:23)
- [signals.rs](crates/oif-sim/src/simulation/signals.rs:35)

这使得真正的信号方块移动后通常能够触发拓扑刷新。电线面板和面附着按 `BlockId` 保存，普通平移不需要改写；旋转时才需要旋转面法线。

表现层也会收集移动前后位置及电线连通区域：

- [incremental.rs](src/scene/incremental.rs:303)

但这些位置会在动画开始阶段立刻刷新，且动画结束阶段没有对应的统一刷新动作，所以刷新时机仍然不符合目标语义。

## 5. 隐含性能问题

### 5.1 Marker 重建可能造成无意义的信号缓存失效

每回合会清除并重建全部静态 marker：

- [markers.rs](crates/oif-sim/src/simulation/markers.rs:7)

marker 的插入和删除会改变 `topology_revision`。即使实际电线网络没有变化，也可能迫使信号缓存重新执行全量连通分量计算。

### 5.2 材料结构每回合全量重建

Turn 结束时会重新扫描材料并重建材料结构：

- [structure_state.rs](crates/oif-sim/src/simulation/structure_state.rs:348)

即使只有少量材料移动，也会产生全世界范围的遍历和连通性计算。

### 5.3 表现实体重复销毁和生成

移动方块和连接受影响的邻居可能被销毁并重新生成。对于大型工厂，这会带来大量实体、子节点和连接网格的重建开销，并且放大动画期间的状态错位。

## 6. 建议的重构方向

### 6.1 模拟输出明确区分状态

Turn 输出应明确包含：

- 本回合开始时用于行为决策的供电结果；
- 所有运动和后处理完成后的最终拓扑版本；
- 运动前后受影响的连接刷新区域；
- 运动动画完成后需要应用的表现刷新事件。

### 6.2 表现层延迟连接刷新

运动开始时只更新实体的位移动画，不根据最终世界重建连接子模型。应该保留本回合开始时的连接表现。

当所有相关 `AnimatedBlock` 和 `AnimatedPusher` 完成后，发送一次连接刷新事件，只刷新运动前后位置、邻居和受影响电线连通分量。

### 6.3 将拓扑版本和表现版本分离

模拟层的 `topology_revision` 只表示实际影响信号网络的拓扑变化。marker、动画状态和表现模型不应直接使信号缓存失效。

表现层可以维护独立的连接表现版本，例如：

- `committed_topology_version`：当前已经显示完成的连接状态；
- `pending_topology_version`：模拟已提交但动画尚未结束的连接状态。

### 6.4 局部刷新材料结构和连接模型

只有发生方块增删、结构移动、焊接变化、附着变化或面板变化的区域才需要刷新。未受影响的结构、信号分量和表现实体应保留。

## 7. 验收标准

- 运动动画期间，连接模型不会按最终位置提前切换；
- 动画结束后，受影响区域的连接模型一次性切换到最终状态；
- 移动结构前后的电线连接都能正确断开或建立；
- 工厂、电线、活塞头和附着子块不会在动画中出现错误连接；
- 运动决策使用的供电状态与表现层最终显示的供电状态语义明确；
- 无拓扑变化时，信号缓存不会因 marker 重建而重复全量刷新；
- 小范围运动不会触发全部材料结构和全部连接模型重建；
- 相同 Turn 在桌面端、移动端和 Web 端得到一致的模拟状态和连接结果。
