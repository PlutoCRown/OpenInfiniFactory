# 运动候选列表与冲突仲裁重构

本文档记录运行时运动 Phase 的目标语义，以及当前实现需要补足的部分。重点是：一个结构在同一回合可以拥有多个运动候选；某个候选失败时，不应因此放弃该结构的其他候选。

## 1. 目标游戏语义

每个运动方块在标记阶段只产生运动意图，不立即修改世界。一个结构可以收到多个不同方向的意图，并按规则形成有序候选列表。例如，一个结构同时接触左右两条传送带时，可以形成：

1. 向左移动，优先级较高；
2. 向右移动，优先级较低。

执行时，候选应逐个尝试：

- 当前候选能够完成整组占格和结构碰撞检查时，提交该候选，并锁定该结构本回合的运动结果；
- 当前候选因为目标格被占用或结构碰撞而失败时，只放弃当前候选，继续尝试该结构的下一个候选；
- 所有候选都失败时，该结构本回合不移动；
- 一个结构一旦成功提交一个候选，其他候选不能再次执行。

因此，“优先向左但向左失败，随后向右成功”是合法且预期的结果。

## 2. 当前实现的实际状态

当前运动计划本身已经是 `Vec<StructureMove>`，并不是每个结构只有一个标签。相同 `StructureId` 可以在列表中拥有多个不同方向或不同来源的运动候选。

`merge_structure_movement_plan` 会合并并排序整个列表，`execute_structure_moves_with_pushers` 再按顺序尝试候选。候选执行失败时会跳过当前项；结构只有在某个候选成功后，才通过已移动或被占用状态阻止后续候选。因此，最基本的“首选失败后尝试次选”目前已经部分成立。

但这个语义目前是由列表顺序和执行过程间接实现的，还不是明确的候选仲裁规则，存在以下风险：

- 没有显式表达“候选失败”和“结构所有候选均失败”是两种不同状态；
- 候选执行过程中会逐步修改真实世界，后续候选看到的是部分提交后的状态；
- 活塞的 `claimed_heads` 主要是活塞头专用的临时占用记录，不能覆盖传送带、升降机、旋转等结构的通用目标占用；
- 部分排序仍依赖坐标或来源坐标，而不是统一的 `BlockID` 稳定决胜规则；
- 如果标记阶段过早把结构标记为已处理，可能会错误地阻止该结构的次选候选进入执行阶段。

## 3. 建议的重构边界

不需要把现有计划强行改成 `HashMap<StructureId, Vec<...>>`。列表适合作为统一的运动候选流；仲裁器可以在处理时按 `StructureId` 建立候选视图，或者使用排序后的连续区间分组。

每个 `StructureMove` 应明确包含以下语义信息：

- 产生意图的 `BlockID`；
- 目标结构的 `StructureId`；
- 位移或旋转结果；
- 运动来源和运动类型；
- 用于排序的优先级；
- 该候选需要占用的完整目标格集合；
- 候选失败后的后续策略，例如尝试次选或保持不动。

建议将运动 Phase 组织为三个阶段：

### 3.1 意图收集

所有运动方块基于 Phase 开始时的世界状态生成候选列表。此阶段只读世界，不修改结构位置、占用格或动画状态。相同结构可以追加多个候选。

### 3.2 候选仲裁

按“运动规则优先级 + 稳定 `BlockID`”处理候选。仲裁器维护通用的临时占用表，而不是只维护 `claimed_heads`：

- 活塞头伸出占用头部目标格；
- 传送带移动占用被运送方块的完整目标 footprint；
- 推动结构占用被推动结构的全部目标格；
- 旋转或其他形变占用变换后的完整格集合。

一个候选只有在其全部目标格可以作为一个原子请求获得预留时才成功。目标格竞争时，由优先级和 `BlockID` 决定胜者，不由当前循环顺序或 `HashMap` 遍历顺序决定。

对每个结构，仲裁器必须保留“当前候选失败后继续尝试下一个候选”的能力。不能因为结构的首选候选失败，就提前把结构整体标记为失败。

### 3.3 统一提交

所有候选确定后，再一次性提交被接受的结构位移、占用格、活塞头状态和动画状态。提交阶段不再重新依赖“谁先写入世界”来决定合法性。

## 4. 代表性冲突

### 两个活塞伸入同一空格

两个活塞都可以在收集阶段生成伸出候选。仲裁阶段只允许一个候选预留该格，例如按较高运动优先级、再按较小 `BlockID` 决胜。另一个候选失败，并执行活塞自身规定的反推或不动作逻辑。

### 两条传送带把方块运向同一格

这不应再通过 `claimed_heads` 特殊处理。两个传送带都生成普通结构运动候选，并竞争同一个目标 footprint。胜者完成移动，败者继续尝试自己的下一个候选；如果没有其他候选，则本回合不移动。

### 一个结构同时受到左右运动

该结构的候选列表例如为“向左、向右”。如果向左的目标格已经被更高优先级的其他结构预留，向左候选失败，但不能锁定该结构。仲裁器应继续尝试向右；只要向右的完整目标 footprint 可用，就接受向右运动。

## 5. 验收标准

- 同一结构可以在一个 Phase 中保留多个方向候选；
- 首选候选失败不会清空或锁死次选候选；
- 一个结构最多成功提交一个运动候选；
- 所有目标格按原子请求仲裁，不能出现结构只移动了一部分的结果；
- 活塞、传送带、升降机、旋转和推动使用同一套通用占用仲裁，`claimed_heads` 只保留为活塞头的领域数据，而不是全局冲突机制；
- 相同条件下结果只由明确的优先级和 `BlockID` 决定，不能依赖坐标遍历顺序、容器遍历顺序或系统调度顺序；
- 同一回合中，候选收集、仲裁和提交之间的边界清晰，便于后续在游戏内加入算法正确性和性能测试。
