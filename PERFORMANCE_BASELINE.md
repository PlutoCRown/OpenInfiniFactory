# 性能基线与复现记录

记录日期：2026-08-13。设备：Apple M4 10 核 GPU，外接 5120×2880 显示器（逻辑 2560×1440，144 Hz）。所有模拟数据使用 Release `oif-debug-http` 串行采集；各表按场景标注采样回合数。

## 测量规则

- 模拟基准必须重新构建 `target/release/oif-debug-http`，禁止复用不明时间的旧二进制。
- 图形基准必须满足 `/perf.frame.window.focused == true`，否则 macOS 可能把窗口节流到约 60 FPS。
- 图形基准记录 `/perf.frame.window` 中的物理尺寸和实际 PresentMode。
- 单类方块先用 `/world/place` 的 AABB 一次性放置，再校验 `placed_count`。
- `/sim/run?n=120` 直接返回 mean、P50、P95、max 和各阶段平均值。

## 空世界 Release 图形基线

测试世界只有一个草地方块，窗口物理尺寸 2560×1440，`AutoNoVsync`。

| 配置 | 焦点 | FPS | 主世界 | 帧间等待 | 判断 |
|---|---|---:|---:|---:|---|
| 阴影 High SSAO、天空盒、默认后处理 | 前台 | 52.5–55.2 | 1.4–1.5 ms | 16.8–17.0 ms | 固定图形管线是瓶颈 |
| 阴影/SSAO/天空盒全关，3D 跟随 UI | 前台 | 287–353 | 1.5–2.0 ms | 1.36–1.50 ms | Present 与普通运行时不是空载瓶颈 |
| 阴影/SSAO/天空盒全关，3D 跟随 UI | 后台/未聚焦 | 约 60 | 约 1.6 ms | 约 15.0 ms | macOS 合成节流，不能作为性能结论 |

结论：用户观察到的“占用不高但空载不足 60 FPS”有两类来源。后台窗口会被系统节流；前台全画质下则确有约 17 ms 的固定图形成本。主世界 ECS/普通运行时不是空载主因。后续按 `GRAPHICS_PERFORMANCE_PLAN.md` 拆 GPU pass 后再实施图形优化。

## 静止传送带模拟基线

场景命令：

```bash
./target/release/oif-debug-http --debug-http=8765 --create-test-free=perf_100k_conveyor
curl -sS -X POST 'http://127.0.0.1:8765/world/place?x=0&y=0&z=0&x1=999&y1=0&z1=99&kind=Conveyor&facing=East'
curl -sS -X POST 'http://127.0.0.1:8765/sim/run?n=120'
```

初始草地占一个格，因此实际放置 99,999 个传送带。

| 版本 | mean | P50 | P95 | movement_mark mean |
|---|---:|---:|---:|---:|
| 信号空集快速路径后、传送带早筛前 | 17.78 ms | 17.75 ms | 18.21 ms | 11.02 ms |
| 工作面为空的传送带在排序前剔除 | 11.00 ms | 10.97 ms | 11.29 ms | 4.01 ms |
| 无材料/无系统设备时跳过 behavior 全表扫描 | 6.29 ms | 6.25 ms | 6.52 ms | 3.90 ms |
| marker 来源/生成物增量计数，空集 O(1) 跳过 | 5.00 ms | 5.01 ms | 5.18 ms | 3.85 ms |

相对第一组可比基线，总耗时平均下降 71.9%，P50 下降 71.8%，P95 下降 71.6%。marker 的回合初与运动后阶段合计由约 1.25 ms 降至约 0.004 ms；behavior 由约 4.54 ms 降至接近计时器噪声。100k 静止工厂方块已经远低于一秒一回合，当前剩余热点是通用运动候选扫描。

## 推杆规模曲线

`/sim/run` 现额外返回 `perf.begin_ms`，用于覆盖结构构建、推杆状态和 deform 图等开局预处理。下表为同一平面内相邻、未通电的 Pusher；它们不会伸出，但会组成一个大型工厂结构。每组推进 3 回合。

| Pusher 数量 | begin | mean/turn | P95/turn |
|---:|---:|---:|---:|
| 109 | 0.13 ms | 0.04 ms | 0.07 ms |
| 1,009 | 0.44 ms | 0.12 ms | 0.18 ms |
| 10,009 | 4.31 ms | 1.28 ms | 1.84 ms |
| 99,999 | 48.22 ms | 16.19 ms | 22.77 ms |

旧实现会为每根推杆枚举至多 12 个同轴同伴的所有切断子集，即每根最多执行 4096 次全结构传播；10,000 个 Pusher 的 `/sim/run` 在 30 秒内无法返回。现在候选固定为“单杆独立”和“全部同轴共动”两类，并在实际发生伸缩时才按结构构建 deform 图。现有互推、对向、共轴、方环等测试全部通过。

Blocker 默认在未通电时伸出，因此属于大量执行器同时改变拓扑的动态压力场景。关闭批量运行中的逐块调试日志后，悬空相邻场景中 1,009 个连续两回合平均约 121.79 ms，10,009 个约 12.01 s；10 万个超过 60 秒。阶段数据表明复杂度主要位于重力与运动裁决，属于共享共轴组及大结构处理的平方级增长。

针对常见的“工厂块直接固定在场景地基上”负载，现按结构维护真实锚定成员，并跳过不可能移动的内部 deform；空头伸出走常数级路径。运动执行还把 `BlockId → actor 坐标` 改为本回合局部索引，并按结构去重 `gravity_held`：

| 有地基 Blocker | 优化前 | 优化后 |
|---:|---:|---:|
| 10,000 | 约 12.41 s/turn | 首回合 8.57 ms，稳定 5.40 ms/turn |
| 100,000 | 超过 60 s/turn | begin 59.36 ms，首回合 87.32 ms，稳定 mean 63.44 ms / P95 68.10 ms |

10,000 常见负载下降约 99.96%。悬空、未锚定且大量互推的 Blocker 网格仍走完整变形语义，保留为下一阶段的极端动态压力测试，不与固定工厂结论混淆。

## 已增强的调试能力

- AABB 批量放置可可靠创建 10k/100k 方块。
- 超过 1000 个放置结果时不再序列化巨大坐标数组，只返回数量和首尾坐标。
- `/sim/run?n=` 返回批次统计与阶段均值，不再只能看到最后一回合。
- `/sim/run?n=` 记录 `begin_ms`，覆盖首回合前结构预处理成本。
- `/sim/run?n=` 关闭逐块模拟日志，避免大量执行器的字符串格式化与日志内存污染阶段耗时。
- 内嵌 `/perf` 返回窗口焦点、实际 PresentMode、物理尺寸和缩放比例。
- `/world/place` 对未知 `kind` 明确报错，不再静默放成兜底材料，避免生成错误基准场景。
