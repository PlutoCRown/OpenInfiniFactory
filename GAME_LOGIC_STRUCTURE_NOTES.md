# 游戏逻辑与运行时结构问题记录

本文档只记录当前实现中需要后续优化的结构问题，以及期望达到的结果。

## 1. `WorldBlocks` 承担了过多状态和不变量

### 当前问题

`WorldBlocks` 公开了大量内部容器、计数器、拓扑版本和临时状态。外部代码可以直接修改这些字段，导致方块表、附着关系、计数器、拓扑版本和临时运行状态之间出现不同步。

### 正确结果

`WorldBlocks` 应该隐藏内部容器，只通过有限的查询和修改接口操作世界。所有插入、删除、移动、清空和拓扑变化，都应由统一入口维护相关不变量；临时运行状态也应与持久世界数据分离。

## 2. 预取缓存会重复克隆完整 `SimSnapshot`

### 当前问题

每个缓存回合都会克隆完整 `SimSnapshot`：[worker.rs](src/sim_bridge/worker.rs:60)、[snapshot.rs](src/sim_bridge/snapshot.rs:12)。世界规模扩大后，缓存深度增加会明显提高内存占用和复制成本。

### 正确结果

预取应尽量复用 worker 内部的连续模拟状态，不为每个回合保存一份完整世界。缓存可以改为保存回合输出、增量变化或轻量级检查点，只在回滚、分支或必要的同步点保存完整快照。

## 3. UI 使用 `unsafe` 访问 Bevy `World`

### 当前问题

`UiAccess` 通过线程局部裸指针暂存 Bevy `World`：[access.rs](src/game/ui/access.rs:31)。当前实现依赖调用者遵守绑定范围和执行顺序；未来调整 UI 调度或引入并行系统后，可能产生难以定位的内存安全和借用问题。

### 正确结果

UI 应通过 Bevy 的 `SystemParam`、资源、消息和显式 UI 命令访问状态，不再通过全局裸指针保存 `World`。UI 生命周期和调用关系应由 ECS 调度保证，而不是依赖注释和调用约定。

## 4. 工厂结构增量连通与可变形子集（已落地主干）

### 已实现

- 加载 / 进世界 / `rebuild_scene`：全量 `rebuild_for_simulation`（连通 + 逻辑头 + `DeformGroup` + `scene_touching`）
- 编辑：`apply_factory_edit` 只重建受影响区域的工厂连通与 deform
- 开局模拟：若已有工厂缓存则 `refresh_for_simulation_start`（材料/验收口），不再全图工厂 BFS
- 已删除 `BreakableSplit` / `breakable_graph` / `pushable` 字段；可推性由 `scene_touching.is_empty()` 派生
- 结构图：有向 `body→head`，无向头—正面（贴脸对向为头—头）；正/反推均对同轴同伴 fork 多候选，按节点数升序优先共轴
- 图 A–D 节点集与 fork 语义见 [docs/spec/deform_groups.md](docs/spec/deform_groups.md)

### 仍待

- 完整候选预留仲裁可继续对齐 [MOVEMENT_CANDIDATE_ARBITRATION.md](MOVEMENT_CANDIDATE_ARBITRATION.md)（当前活塞已按 DeformGroup + `held_blocks` 冲突）

环上单切不成桥时：改用切断全部体–头边后的无向可达（与旧 multi-cut 等价），使对向环上「一正一反」共轴。

运行时的普通场景结构和工厂结构不应因为空间上再次接触而自动合并或分解。电线连接使用独立的动态网络计算；活塞伸出属于单个工厂结构的形变，不代表结构被切成两个结构。

## 5. 同一 Phase 内的运动冲突需要预留和仲裁

### 当前问题

同一回合中，两个活塞可能同时试图把头伸入同一个目标格。回合开始时目标格为空，因此两个活塞的伸出判断都合法；如果在真正执行时才依次写入世界，后执行的活塞才会发现目标格已经被占用。这样会让结果依赖执行顺序，而不是依赖统一的回合规则。

当前实现已经有 `claimed_heads` 这样的临时头格占用记录，可以在标记阶段提前发现头格竞争；但目前运动方块主要按坐标顺序处理，而且预留主要覆盖活塞头，尚未抽象成所有结构运动都能使用的原子占用仲裁。

运动方块的竞争顺序应使用稳定的 `BlockId`，不应依赖 `HashMap` 遍历顺序或方块坐标顺序。

### 正确结果

运动 Phase 应拆成“意图收集、占格仲裁、统一执行”三个步骤：

1. 所有活塞基于同一个 Phase 开始快照收集伸出、收回、推动和反推意图，不修改真实世界；
2. 建立临时预留表，记录每个目标格由哪个 `BlockId` 申请；同一目标格发生竞争时，按 `BlockId` 稳定选择胜者；
3. 胜者预留目标格，败者按照活塞规则执行失败分支，例如反推自身或保持不动；
4. 所有意图确定后，再统一提交结构位移、活塞头占格和动画状态；提交阶段不再重新依赖执行顺序判断合法性。

对于两个活塞同时伸入同一空格的情况，较小 `BlockId` 的活塞获得该格预留，另一个活塞的伸出意图失败，并进入既定的反推或不动作逻辑。若胜者后续因为结构整体碰撞而无法执行，应释放预留并重新按 `BlockId` 尝试下一候选意图，或明确规定该回合该目标格无人执行；不能让失败结果通过执行顺序随机改变。

结构推动不能只预留活塞头目标格，还应把被推动结构的全部目标格作为一组原子占用请求。只有整组目标格都能通过仲裁，该推动意图才能提交。
